{
	"name": "@aegis/core",
	"version": "0.1.0",
	"description": "Pure logic: policy engine, event model, routing decisions — zero dependencies",
	"type": "module",
	"main": "./dist/index.js",
	"types": "./dist/index.d.ts",
	"exports": {
		".": {
			"types": "./src/index.ts",
			"import": "./dist/index.js"
		}
	},
	"files": [
		"dist"
	],
	"scripts": {
		"build": "tsup src/index.ts --format esm --dts --clean",
		"typecheck": "tsc --noEmit",
		"clean": "rm -rf dist"
	},
	"devDependencies": {
		"fast-check": "^3.23.2"
	},
	"license": "Apache-2.0"
}
