/**
 * aegis stats — Statistics command.
 *
 * Displays session statistics: context savings, tool call counts,
 * cache hit rates, and performance metrics.
 *
 * Implementation deferred to Phase 1.
 */

export const COMMAND_NAME = "stats" as const;
export const COMMAND_DESCRIPTION = "Show session statistics and context savings";
