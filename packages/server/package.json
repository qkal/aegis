{
	"name": "@aegis/server",
	"version": "0.1.0",
	"description": "MCP server: tool registration, transport, hook orchestration, lifecycle management",
	"type": "module",
	"main": "./dist/index.js",
	"types": "./dist/index.d.ts",
	"exports": {
		".": {
			"types": "./src/index.ts",
			"import": "./dist/index.js"
		}
	},
	"files": [
		"dist"
	],
	"scripts": {
		"build": "tsup src/index.ts --format esm --dts --clean",
		"typecheck": "tsc --noEmit",
		"clean": "rm -rf dist"
	},
	"dependencies": {
		"@aegis/core": "workspace:*",
		"@aegis/engine": "workspace:*",
		"@aegis/storage": "workspace:*",
		"@aegis/adapters": "workspace:*"
	},
	"license": "Apache-2.0"
}
