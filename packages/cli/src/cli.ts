/**
 * Aegis CLI entry point.
 *
 * Minimal command routing. Each command is defined in its own file
 * under ./commands/. This file handles argument parsing and dispatch.
 *
 * Implementation deferred to Phase 1.
 */

export const CLI_NAME = "aegis" as const;
export const CLI_VERSION = "0.1.0" as const;
export const CLI_DESCRIPTION = "Context infrastructure engine for AI coding agents";
